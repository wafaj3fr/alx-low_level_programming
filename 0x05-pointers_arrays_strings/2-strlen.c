#include "main.h"

/**
 * _strlen - returns the length of a string.
 * @s: string to be calculated
 * Return: length of the string
 */

int _strlen(char *s)
{
	int len;
	int n;

	*s = s[n];
	len = sizeof(s[n]) / sizeof(s[0]);

	return (len);
}
